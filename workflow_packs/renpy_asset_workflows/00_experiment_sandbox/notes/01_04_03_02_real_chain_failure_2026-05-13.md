# 01 → 04 → 03 → 02 real-chain failure note — 2026-05-13

## Evidence

A full chain was executed using the active numbered workflow JSONs in this order:

```text
01_character_anchor_and_prompt
→ 04_pose_variation_reference_and_regeneration
→ 03_expression_variation_face_composite
→ 02_toonout_transparency_alpha
```

Run folder:

```text
/mnt/c/Users/Desktop/Documents/ComfyUI/output/hermes_vn_full_chain_01_04_03_02
```

Contact sheet:

```text
/mnt/c/Users/Desktop/Documents/ComfyUI/output/hermes_vn_full_chain_01_04_03_02/contact_01_04_03_02_chain.png
```

Prompt IDs:

```text
01: bcfd0b49-3a9e-44a4-974a-7606e4fccd2b
04: 37fa58b6-db42-4006-9595-76b38d6bc295
03: e6dff94e-f7e0-45e0-b7a3-e4ac666dcde9
02: 854d72e8-fa40-4d3e-8bd2-781e270e1f8a
```

## Findings

The workflows execute technically, but the chain does not meet production/canonical quality.

1. 01 generated source is not a flat/simple neutral gray background.
   - It includes scene-like lighting/shadow/background detail.
   - The current positive prompt includes `BREAK depth_of_field, volumetric_lighting`, which conflicts with a plain sprite source background.

2. 04 creates/strengthens a bright/white exterior rim and linework/color drift.
   - The issue appears in the 04 source before 02 alpha.
   - Therefore 02 is not the root cause; it inherits contaminated 04 edge pixels.
   - Likely contributors: high `IPAdapterAdvanced weight=0.65`, `weight_type=style transfer`, BiRefNet `attn_mask` blur=3, prompt linework tags, and OpenPose+empty-latent regeneration changing the full image.

3. 03 expression change is too weak in the full chain.
   - The 03 face-local composite route technically runs, but on the 04 output the happy expression is not sufficiently visible.
   - The current route was promoted from source-specific expression tests and is not proven after 04 pose regeneration.

4. 02 alpha output is RGBA and technically transparent, but it exposes the 04 rim/edge contamination.
   - Final file is RGBA 1152x1536.
   - The visible halo is caused upstream by 04 source pixels, not solely by the 02 matting node.

## Root cause summary

Previous validation was not a true end-to-end `01 → 04 → 03 → 02` chain with the active numbered workflows. It relied on partial/sandbox outputs and accepted intermediate tests. That was insufficient.

Current status:

```text
01: runnable, but background is not reliably simple gray
04: runnable, but not production-safe due to rim/style/color drift
03: runnable, but not production-safe after 04 because expression change is weak
02: runnable alpha tool, but cannot fix upstream rim/edge contamination
```

## Do not claim

Do not claim 04 or 03 are production-ready from the previous smoke tests. They are executable templates only until a full-chain QA passes.

## Next minimal debugging route

One variable at a time:

1. 01 background fix first:
   - remove `BREAK depth_of_field, volumetric_lighting`
   - test plain background prompt variants with fixed seed
   - target: flat/simple gray source before any downstream workflow

2. 04 edge/rim fix second:
   - use clean 01 source
   - compare current `mask_blur=3` vs `mask_blur=1` vs lower IPAdapter weight
   - do not judge 02 until 04 source has no obvious white rim

3. 03 expression fix third:
   - test on the accepted 04 source
   - increase expression readability or revise face-mask/inpaint settings
   - accept only if composited output clearly differs from 04 source

4. 02 last:
   - run alpha on accepted 03 output
   - check light/dark composites
